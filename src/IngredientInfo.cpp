#include <iostream>
#include <vector>
#include <map>
#include <sstream>
#include "../include/IngredientInfo.h"

vector<map<string, string>> IngredientInfo::parseData(string ingredientInfo){
    vector<map<string, string>> x;
    return x;
}